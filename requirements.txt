# Kokoro TTS runtime for claude-ai-notifs. This file is the human-edited SOURCE
# of direct dependencies; setup.sh installs from the generated, hash-locked
# requirements.lock (full transitive tree, SHA-256 per artifact, --require-hashes).
# After editing a pin here, regenerate the lock and re-test:
#   uv pip compile requirements.txt --generate-hashes --universal -o requirements.lock
#   ./setup.sh --test
kokoro-onnx==0.4.7
soundfile==0.14.0
