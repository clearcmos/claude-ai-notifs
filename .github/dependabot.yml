version: 2

# Python updates remain manual because requirements.lock is generated from
# requirements.txt with uv pip compile. CI rejects direct-pin drift, but an
# automated bump cannot regenerate this custom hash-lock workflow.
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
